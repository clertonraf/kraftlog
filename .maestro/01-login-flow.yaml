appId: org.reactjs.native.example.kraftlog
---
# Simple App Launch Test
# Tests: Verify app launches and shows some content

- launchApp
- waitForAnimationToEnd
- waitForAnimationToEnd

# Check if anything is visible (either login or home screen)
- assertVisible:
    text: ".*"
